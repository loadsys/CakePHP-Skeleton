/**
 * This file contains any directive that should be executed **on every page**.
 * This is the last script to be loaded before </body> (and before any
 * page-specific scripts. Calls to global functions should happen here.
 *
 * Anything that needs to happen during window.onload() should be placed in
 * init.js.
 */
