#!/usr/bin/env bash

DIR="$( cd -P "$( dirname "$0" )"/.. && pwd )";

git fetch --all --tags --prune

# Update repository
if [ $1 ]; then
	git checkout $1
else
	git pull
fi

git submodule update --init --recursive

$DIR/bin/migrations
